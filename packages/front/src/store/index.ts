import { createStore } from 'vuex'
import AuthUser from './modules/AuthUser'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    AuthUser
  }
})
