#%%

import pandas as pd

df = pd.read_csv('./data/urls.csv')
df2 = df.copy()
df['is_edge'] = False

for index, row in df.iterrows():
    # print(row[])
    data.fChi2.loc[idx] = fChi2



# df
# r = df.loc[df2['url'].isin(df.url.values) & df2.id != df.id]
# r
# df
# df
# (df['id'] != 6) & (
# r

# print(r)
